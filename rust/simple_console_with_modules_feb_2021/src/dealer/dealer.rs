
use std::vec::Vec;

use crate::player::player::Player;

pub fn deal_hands(players: Vec<Player>) { 
    println!("TRACER hello from deal_hands");
/*
    let mut counter = 1;
    for player in &mut players {
        counter += 1;
    }
*/
}
